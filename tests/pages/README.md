# Page Objects

This folder is reserved for Playwright Page Object Model classes introduced in Phase 2.

Naming convention:

- Class names use `PascalCase` and end with `Page`
- File names use `kebab-case` when practical, or match class name for clarity

All locators should live in page objects, not in spec files.
